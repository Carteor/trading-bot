models:
  - name: daily_market_summary
    description: "Analysis-ready mart table with technical indicators, one row per symbol per day"
    columns:
      - name: symbol
        description: "Stock ticker symbol e.g. AAPL, MSFT"
        tests:
          - not_null
      - name: date
        description: "Trading date"
        tests:
          - not_null
      - name: close
        description: "Closing price"
        tests:
          - not_null
      - name: ma_7
        description: "7-day moving average of closing price"
      - name: ma_21
        description: "21-day moving average of closing price"
      - name: rsi_14
        description: "14-day Relative Strength Index"
      - name: daily_return
        description: "Day over day percentage change in closing price"
      - name: volatility_21
        description: "21-day rolling standard deviation of daily returns"
